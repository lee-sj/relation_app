
<%= bootstrap_form_for @participate do |f| %>
    <%= f.select :artist_id, options_for_select(@artists.collect{ |artist| [artist.name, artist.id] }) %>
    <%= f.select :song_id, options_for_select(@songs.collect{ |song| [song.title, song.id] }) %>
    <%= f.submit %>

<% end %>